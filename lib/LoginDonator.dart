import 'package:flutter/material.dart';

class LoginDonator extends StatelessWidget {
  @override

  Widget build(BuildContext context) {
    return MaterialApp(
      title: "loginhemocentro",
      home: Scaffold(
        appBar: AppBar(title: Text("Tela login doador"),),
        body: Center(
        ),
      ),
    );
  }
}
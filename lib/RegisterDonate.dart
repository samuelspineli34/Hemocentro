import 'package:flutter/material.dart';

class RegisterDonate extends StatelessWidget {
  @override

  Widget build(BuildContext context) {
    return MaterialApp(
      title: "registrodoador",
      home: Scaffold(
        appBar: AppBar(title: Text("Tela registro doador"),),
        body: Center(
        ),
      ),
    );
  }
}